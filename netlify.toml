[build]
  publish = "build"
  functions = "functions"

[build.environment]
  PIPENV_VENV_IN_PROJECT = "true"

[build]
  command = "pip install -r requirements.txt && npm run build"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200